// export const add = (a, b) => a + b;
// export const subtract = (a, b) => a - b;

const add = (a, b ) => {
    return a + b;
};

const subtract = (a, b ) => {
    return a - b;
};

export default {
    add,
    subtract,
};